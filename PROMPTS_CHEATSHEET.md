# 🤖 คู่มือคำสั่งสำหรับสั่งงาน AI (Prompt Cheatsheet)

เอกสารนี้รวบรวม "คำสั่งเฉพาะ (Custom Prompts)" ที่เราได้ตกลงและสร้างกฎร่วมกันไว้ เพื่อให้คุณสามารถพิมพ์สั่งงานผม (AI) ได้ทันทีโดยไม่ต้องอธิบายยาวๆ ครับ

---

## 📦 การจัดการแพ็กเกจ (Package Deployment)

### คำสั่ง: `"push package"`
**สิ่งที่ผมจะทำ:**
1. รวบรวมไฟล์ที่ใช้สำหรับนำไปติดตั้งบนโปรดักชั่น (ไฟล์รัน `.exe/binary`, ไฟล์ `templates`, ไฟล์ `.env.example`, และไฟล์ `*.service`) ของทั้ง `fpsso` และ `fp-radius`
2. นำไฟล์เหล่านั้นไปใส่ในโฟลเดอร์ `/var/www/api/package_send` (ซึ่งผูกกับ Repository `internet-authen` ไว้)
3. ตรวจสอบให้แน่ใจว่า **ไม่มีไฟล์ Source Code (`.pas`)** หลุดติดไปด้วยเด็ดขาด
4. ทำการ `git add`, `git commit` และ `git push` ขึ้น GitHub อัตโนมัติ

**ตัวอย่างการพิมพ์บอก AI:**
- *"ช่วย push package อัปเดตล่าสุดให้หน่อย"*
- *"push package"*

---

*(หากในอนาคตมีคำสั่งหรืองานอะไรที่ต้องทำซ้ำๆ บ่อยๆ สามารถบอกให้ผม "ตั้งกฎ" และนำมาจดเพิ่มในไฟล์นี้ได้ตลอดเลยนะครับ!)*
